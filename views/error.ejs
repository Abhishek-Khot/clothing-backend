<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error <%= error.status || 500 %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-lg shadow-md text-center max-w-md w-full">
        <div class="text-6xl font-bold text-red-500 mb-4">
            <%= error.status || 500 %>
        </div>
        <h1 class="text-2xl font-bold text-gray-800 mb-4">
            <%= message || 'Something went wrong!' %>
        </h1>
        <p class="text-gray-600 mb-6">
            We're sorry, but an error occurred while processing your request.
        </p>
        <a href="/" class="inline-block bg-blue-500 text-white px-6 py-2 rounded-md hover:bg-blue-600 transition duration-200">
            Go to Home
        </a>
        
        <% if (error.stack && process.env.NODE_ENV === 'development') { %>
            <div class="mt-8 p-4 bg-gray-100 rounded-md text-left text-sm text-gray-700 overflow-x-auto">
                <pre><code><%= error.stack %></code></pre>
            </div>
        <% } %>
    </div>
</body>
</html>
